export const Router = (socket) => (command) => {
  switch (command) {
    case "feedupdate":
      // call feedmachine
      break;
    case "maintain":
      // call maintain machine
      break;
    default:
      break;
  }
};
